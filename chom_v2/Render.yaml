services:
  - type: web
    name: chom-unfiltered
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m gunicorn app:app -c gunicorn.conf.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0